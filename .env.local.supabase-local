# Local Supabase environment — safe for development and staging.
# Generated after: supabase init (supabase/ directory created 2026-05-10)
#
# SAFETY: This file targets 127.0.0.1:54321 (local Docker containers).
#         It is NOT the production Supabase project.
#         Production credentials remain in .env.local (do not copy from there).
#
# These values are the well-known public defaults printed by `supabase start`.
# They are identical across all local Supabase installations and are documented
# at https://supabase.com/docs/guides/local-development — not a secret.
#
# TO USE:
#   1. Ensure Docker Desktop is installed and running
#   2. Run: cd agent-1 && supabase start
#   3. Verify the URL and key match what supabase start prints
#   4. Activate: cp .env.local.supabase-local .env.local
#   5. After import: restore production: cp .env.local.production.bak .env.local
#
# NEVER run B2B_IMPORT_CONFIRM=true unless SUPABASE_URL contains 127.0.0.1

SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hj04zWl196z2-SBc0
MATCH_API_KEY=local-test-match-key
RECRUITER_API_KEY=local-test-recruiter-key
AGENT_API_KEY=local-test-agent-key
